{% load tz %}Hi {{ booking.name }},

Your {{ booking.appointment_type.name }} with {{ site_name }} is confirmed.

When:     {{ booking.start_at|date:"l, F j, Y" }} at {{ booking.start_at|date:"H:i" }} UTC
          ({{ booking.appointment_type.duration_minutes }} minutes)
{% if booking.customer_timezone %}Your TZ:  {{ booking.customer_timezone }}{% endif %}
{% if booking.appointment_type.location_instructions %}Where:    {{ booking.appointment_type.location_instructions }}{% endif %}

We have attached a calendar invite (.ics) you can add to your calendar.

Need to change or cancel? Use this private link:
{{ manage_url }}

Thanks,
{{ site_name }}
{{ site_url }}
